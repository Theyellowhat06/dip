class Item {
  final String id, name, type, price, color, size, mark, company, date;

  Item(this.id, this.name, this.type, this.price, this.color, this.size,
      this.mark, this.company, this.date);
}
